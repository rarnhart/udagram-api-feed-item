apiVersion: v1
kind: Service
metadata:
  name: udagram-api-feed-item-service
  labels:
    run: udagram-api-feed-item-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    run: udagram-api-feed-item-service
